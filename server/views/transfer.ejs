<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transfer Money</title>
</head>

<body>
    <h1>Transfer Money</h1>
    <div>
        <p><strong>Username:</strong> <%= account.username %></p>
        <p><strong>ID:</strong> <%= account.id %></p>
        <p><strong>Balance:</strong> <%= account.balance %></p>
    </div>
    <div>
        <form action="/account/transfer" method="POST">
            <!-- Hidden input field to set the default value for fromAccountId -->
            <input type="hidden" name="fromAccountId" value="<%= account.id %>">
            
            <label for="toAccountId">To Account ID:</label>
            <input type="text" id="toAccountId" name="toAccountId" required><br><br>
            
            <label for="amount">Amount:</label>
            <input type="number" id="amount" name="amount" required><br><br>
            
            <button type="submit">Transfer</button>
        </form>
    </div>
    <a href="/profile?token=<%= token %>"><button>Back</button></a>

</body>

</html>
